block config

doctype html
html(lang=__.lang, class="no-js")
    head
        block head
            include head
    body.body(class="admin")
        include mixins
        include blocks/popups
        .content__wrap
            include blocks/header-buys
            .content.content--admin
                block content
        include blocks/footer 
        block scripts-config
        block scripts
            include scripts